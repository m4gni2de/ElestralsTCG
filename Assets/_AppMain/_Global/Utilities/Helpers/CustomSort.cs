using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Reflection;

public static class CustomSort
{
    //public static List<T> SortBy<T>(this List<T> list, string propName)
    //{
    //    List<T> sorted = new List<T>();

    //    for (int i = 0; i < list.Count; i++)
    //    {
    //        object val = list[i].GetType().GetProperty(propName).GetValue(list[i]);
    //    }
    //}

    //public static IComparer SortBy(this IList list, string propName)
    //{

    //}

    
}
